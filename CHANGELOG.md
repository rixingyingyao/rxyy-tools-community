# Changelog

## 0.2.3

- Codex 原生通道闲置释放后，显式派发可按需重新连接；普通列表刷新不会重连已结束的任务。
- 修复长任务完整快照超过旧接收上限时反复重连的问题，并为超限错误增加等待时间。
- 已绑定的空白 Codex 任务可沿用保存的模型启动首轮；不支持的首轮附件明确报错。
- 补充连接、首轮发送与回执回归，保留项目历史别名对应的任务数据。

## 0.2.2

- Codex 桌面与手机页支持图片、文件和持久发送回执，运行中的原生任务可追加附件。
- 同一 Codex 任务换用新的 MCP 连接标识时复用既有标签；保留历史重复标签，不自动删除或合并记录。
- 兼容带命名空间的原生异步问题格式，修正手机深色主题问题卡的对比度。
- 补充社区隔离启动、原生投递、重连身份与问题卡交互回归。

桌面协议以本机 Codex/Cursor 实际版本为准；本地输出同步不调用模型，发送消息后模型执行仍按宿主计额。
